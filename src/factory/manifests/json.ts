import type { IManifest } from "./manifest";

export const json: IManifest = {
  languageOptions: {
    parser: null,
    globals: null,
    allowTrailingCommas: true,
  },
  parserOptions: {
    parser: null,
  },
  language: "json/jsonc",
};
