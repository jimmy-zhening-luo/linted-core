import type { IManifest } from "./manifest";

export const jsoncc: IManifest = {
  languageOptions: {
    parser: null,
    globals: null,
  },
  parserOptions: {
    parser: null,
  },
  language: "json/jsonc",
}
